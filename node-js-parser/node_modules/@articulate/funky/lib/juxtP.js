var compose = require('ramda/src/compose')
var juxt    = require('ramda/src/juxt')

var all = require('./all')

// juxtP :: [a... -> Promise b] -> a... -> Promise [b]
var juxtP = function (fs) { return compose(all, juxt(fs)); }

module.exports = juxtP
