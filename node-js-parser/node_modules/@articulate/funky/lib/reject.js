var bind    = require('ramda/src/bind')
var compose = require('ramda/src/compose')
var is      = require('ramda/src/is')
var unless  = require('ramda/src/unless')

// reject :: a -> Promise Error
var reject = compose(bind(Promise.reject, Promise), unless(is(Error), Error))

module.exports = reject
