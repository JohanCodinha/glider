var curry  = require('ramda/src/curry')
var insert = require('ramda/src/insert')
var remove = require('ramda/src/remove')

// move :: Number -> Number -> [a] -> [a]
var move = function (from, to, list) { return insert(to, list[from], remove(from, 1, list)); }

module.exports = curry(move)
