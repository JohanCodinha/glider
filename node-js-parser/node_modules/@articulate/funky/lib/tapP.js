var always = require('ramda/src/always')
var curry  = require('ramda/src/curry')

// tapP :: (a -> Promise b) -> a -> Promise a
var tapP = function (fn, x) { return Promise.resolve(x).then(fn).then(always(x)); }

module.exports = curry(tapP)
